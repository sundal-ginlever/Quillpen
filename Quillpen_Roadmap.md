# 🗺️ Quillpen 로드맵 (보류 아이템)

> 2026-07-17 전체 코드 감사에서 도출된 후속 과제 목록입니다.
> 즉시 수정 가능한 결함은 모두 반영 완료되었고, 아래는 설계 판단이나 트레이드오프 검토가 필요해 보류한 항목입니다.
> 우선순위와 규모를 함께 표기했으니 착수 시 참고하세요.

---

## 1. 모바일 UX 재설계 — 우선순위 ★★★

현재 모바일에서는 (a) 위젯 본문 터치가 곧바로 이동 드래그로 시작되어 메모 스크롤·편집과 충돌하고, (b) 작은 화면에서 무한 캔버스 자체의 가독성이 떨어짐.

| 단계 | 내용 | 규모 |
|---|---|---|
| **1단계: 조작 충돌 해소** | 모바일에서 위젯 이동은 상단 드래그바를 잡았을 때만 시작. 본문 터치는 스크롤·편집에 양보 | 작음 |
| **2단계: 모바일 포커스 모드** | 위젯을 탭하면 전체 화면으로 확대해 보기/편집, 닫으면 캔버스 복귀. 캔버스는 "지도" 역할 | 중간 (권장 핵심 투자) |
| **3단계: 리스트 뷰** | 좁은 화면에서 위젯 목록(제목·미리보기)으로 보는 전환 토글 | 중간 (2단계 사용 후 결정) |
| 보조 | `maximum-scale=1` 제거(핀치 확대 접근성), 터치 타겟 44px 보장, 미니맵 기본 표시 | 작음 |

**추천 경로**: 1단계 → 2단계 순차 진행.

---

## 2. 공유 토큰 보안 강화 (RPC 전환) — 우선순위 ★★☆

**문제**: 현재 RLS 정책은 클라이언트의 토큰 필터를 강제할 수 없어, 공개된 anon 키만으로
`q_canvases` 전체 조회 시 **공유가 켜진 모든 캔버스와 share_token이 통째로 노출**됨
(위젯 내용도 동일하게 열람 가능).

**해결**: 테이블 직접 조회 정책을 제거하고 SECURITY DEFINER RPC 함수
(`get_shared_canvas`, `get_shared_widgets`)로만 조회 허용.
마이그레이션 SQL은 `supabase_setup.sql` 하단에 주석으로 준비되어 있음.

**주의 (세트 작업)**:
- SQL만 실행하면 공유 기능이 즉시 고장남 — `js/share.js`의 직접 조회를 RPC 호출로 함께 전환해야 함
- 공유 뷰어의 Realtime 실시간 갱신을 잃음 (뷰어는 새로고침해야 최신 내용 확인)

**판단 기준**: 민감한 내용을 공유할 일이 생기면 적용. 개인 사용 수준에서는 현상 유지 가능.

---

## 3. IndexedDB 하이브리드 전환 — 우선순위 ★☆☆

**배경**: base64 이미지가 localStorage 5MB 한계를 압박할 수 있음.
단, 감사 수정으로 캔버스 드롭 이미지도 Supabase Storage 업로드로 통일되어
로그인 상태에서는 압박이 크게 줄어든 상태.

**주의사항**:
- `beforeunload`의 동기 localStorage 저장이 현재 데이터 안전망의 핵심 — IndexedDB는 비동기라 탭 닫기 직전 쓰기를 보장할 수 없음
- 전면 전환보다 **하이브리드 권장**: 위젯 상태는 localStorage 유지, 대용량 blob만 IndexedDB
- 착수 시 "수시 미리 저장" 구조로 재설계 필요

---

## 4. FortuneSheet 엔진 전환 — 우선순위 ★☆☆ (대공사)

멀티 시트/고급 수식을 위한 스프레드시트 엔진 교체.
React 기반이라 빌드 도구 도입 또는 무거운 UMD 번들이 필요하고, 기존 Jspreadsheet
데이터(`jdata`/`jstyle`/`jmerge`) 마이그레이션도 수반됨.
현재 Jspreadsheet CE v4가 안정적으로 동작 중이므로, 멀티 시트 위젯이 실제로
아쉬워질 때 별도 프로젝트로 진행 권장.

---

## 5. 기타 소규모 항목

- **Storage 버킷 정책 SQL화**: `quillpen-images` 버킷 생성·정책이 `supabase_setup.sql`에 없음. 업로드가 실패해 base64 폴백으로 빠진다면 이것부터 확인
- **이미지 휴지통(soft delete)**: 현재는 삭제 → 클라우드 동기화 완료 후 Ctrl+Z 시 이미지 파일이 이미 지워진 상태. 완전한 복원을 원하면 휴지통 개념 필요
- **UNDO_LIMIT(30) 상향 또는 압축**: 대형 캔버스에서 히스토리가 빨리 소진될 수 있음

---

## 📌 배포 참고사항 (1회성)

- SW 캐시가 v2로 변경됨 — 배포 후 새로고침 2회면 새 버전 적용
- 저장 형식 변경으로 업데이트 직후 "동기화되지 않은 이전 로컬 버전" 경고가 1회 뜰 수 있음 → 데이터 이상 아님, **"서버 유지"** 선택하면 됨
